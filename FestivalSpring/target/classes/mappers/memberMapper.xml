<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="com.spring.festival.mapper.memberMapper">

     <select id="selectMember" resultType="memberVO">
        SELECT * FROM member
     </select>
     
     <select id="idDupleCheck" resultType="int">
        SELECT COUNT(*) FROM member where m_id = #{id}
     </select>
     
     <select id="nickDupleCheck" resultType="int">
        SELECT COUNT(*) FROM member where m_nickname = #{nick}
     </select>

     <insert id="insertMember">
     			INSERT INTO member(m_id, m_pw, m_name, m_nickname, m_phonenum, m_email, m_birth) 
     							VALUES(    #{id} 
				                 		 , sha2(#{password},512)
				                 		 , #{name}
				                 		 , #{nick}
				                 		 , #{phone}
				                 		 , #{email}
				                 		 , #{birth})
     </insert>
</mapper>